---
// En-tête de page interne : titre + sous-titre, sur fond bleu profond.
interface Props {
  surtitre?: string;
  titre: string;
  sousTitre?: string;
}
const { surtitre, titre, sousTitre } = Astro.props;
---

<section class="bg-marine text-white">
  <div class="mx-auto max-w-content px-5 py-16 sm:py-20">
    {surtitre && <p class="mb-3 text-sm font-semibold uppercase tracking-widest text-ocre-light">{surtitre}</p>}
    <h1 class="font-serif text-3xl font-bold leading-tight sm:text-4xl md:text-5xl">{titre}</h1>
    {sousTitre && <p class="mt-4 max-w-2xl text-lg text-white/80">{sousTitre}</p>}
  </div>
</section>
